<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MinhasIdeias.View.IdeasPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="New Idea" Activated="ToolbarItem_Activated" />
    </ContentPage.ToolbarItems>
    <StackLayout>
        <ListView x:Name="IdeasListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Height="30">
                        <StackLayout Orientation="Horizontal">
                            <Label ClassId="{Binding Id}">
                                <Label.Text>Editar</Label.Text>
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="Edit" />
                                </Label.GestureRecognizers>
                            </Label> 
                            <StackLayout HorizontalOptions="StartAndExpand">
                                <Label Text="{Binding Description}"></Label>
                                <Label Text="{Binding Comment}"></Label>
                            </StackLayout>
                            <Label ClassId="{Binding Id}">
                                <Label.Text>Excluir</Label.Text>
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="Delete" />
                                </Label.GestureRecognizers>
                            </Label>
                        </StackLayout>
                    </ViewCell>
                    <!--<TextCell Text="{Binding Description}" Detail="{Binding Comment}" /> -->
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>